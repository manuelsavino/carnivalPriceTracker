const router = require("express").Router();
const cors = require("cors");
const cruisSeachController = require("../controllers/cruiseSearchController");
// router.route("/").get(cors(), cruisSeachController.getCruises);

module.exports = router;
